name: Build Email Service

on:
  pull_request:
  push:
    branches: ['main']
    tags: ['v*']
    paths-ignore:
      - 'infra/k8s/**'

concurrency:
  group: '${{ github.workflow }} @ ${{ github.ref }}'
  cancel-in-progress: true

jobs:
  docker:
    permissions:
      contents: read
      packages: write
      security-events: write
    uses: USA-RedDragon/reusable-actions/.github/workflows/docker-ci.yaml@8a4b70987629154883ec4da1f523c70907b67059 # v2.0.12
    with:
      push: ${{ github.event_name != 'pull_request' }}
